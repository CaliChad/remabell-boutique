# Paystack API Configuration
# Copy this file to .env.local and add your actual keys

# Public key (safe for client-side use)
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=your_public_key_here

# Secret key (server-side only - NEVER expose publicly)  
PAYSTACK_SECRET_KEY=your_secret_key_here

# Webhook secret (optional - for verifying webhook signatures)
# PAYSTACK_WEBHOOK_SECRET=your_webhook_secret

# Site URL (update for production)
NEXT_PUBLIC_SITE_URL=https://yourdomain.com
